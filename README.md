# CLI-simulator
Simplified C++ command-line interpreter implementing core Unix-style functionality. Supports built-in commands like touch, wc, tr, and head, with a custom tokenizer, parser, and interpreter handling arguments, redirection, pipes, and batch input. Implements all components through an object-oriented architecture. 
